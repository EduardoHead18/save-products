name: app-actions
on:
  push:
    branches:
      - develop
jobs:
  app-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: save jar artifact
        uses: actions/upload-artifact@v4
        with:
          name: product-api-jar
          path: target/products-apirest-0.0.1-SNAPSHOT.jar

  ec2-deploy:
    needs: app-build
    runs-on: ubuntu-latest
    steps:
      run: |
        sudo ssh -i ${{ secrets.EC2_KEY }} ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }}
        echo "hello from server"
        pwd
#      - name: Download artifact from GitHub Actions
#        uses: actions/download-artifact@v4
#        with:
#          name: product-api-jar
#          path: .

#      - name: Transfer artifact to EC2
#        uses: appleboy/scp-action@v0.1.7
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USER }}
#          key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          source: "products-apirest-0.0.1-SNAPSHOT.jar"
#          target: "/home/${{ secrets.EC2_USER }}/app/"


#      - name: Deploy on EC2
#        uses: appleboy/ssh-action@v0.1.7
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USERNAME }}
#          key: ${{ secrets.EC2_KEY }}
#          script: |
#            echo "✅ Deploying API in EC2..."
#            cd /home/${{ secrets.EC2_USER }}/app/
#            chmod +x products-apirest-0.0.1-SNAPSHOT.jar
#            nohup java -jar products-apirest-0.0.1-SNAPSHOT.jar > log.txt 2>&1 &
#            echo "🚀 API is running!"